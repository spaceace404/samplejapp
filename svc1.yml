apiVersion: v1
metadata:
  name: svc1
spec:
  type: NodePort  
  selector: 
    role: app
  ports:
    - port: 80     ## Virtual LB Port, you can define any port of your choice
      targetPort: 3000  ## port number inside container
      #nodePort: 30045   ## range of node ports is 30000 - 32767
    - port: 8080     ## Virtual LB Port, you can define any port of your choice
      targetPort: 3001  ## port number inside container
